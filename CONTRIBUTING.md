# 参与贡献

感谢你愿意改进 Waystone。提交前请先读完这页。

## 开发环境

- Python 3.11 及以上，推荐用 [uv](https://docs.astral.sh/uv/) 管理依赖
- 可选：Docker，并提前执行 `docker pull caddy:2`，这样反向代理转发测试会在容器里实跑，否则自动跳过

```bash
uv sync --extra test
uv run pytest -q
```

测试不需要真实的 Mem0：服务端测试使用内存里的假向量后端。`deploy/smoke.py` 需要连接真实 Mem0，只在部署到服务器后运行。

## 提交要求

- **先写测试**：修 bug 先补一个能复现问题的失败测试；新功能附带覆盖主要分支的测试。
- **小步提交**：一个 PR 只做一件事，说明“解决了什么问题、怎么验证的”。
- **保持现有写法**：代码沿用仓库现有的紧凑风格；注释写“为什么这样做”，不复述代码做了什么；改了逻辑要同步改相邻注释。
- **不要提交**：密钥、会话文件、数据库、备份，以及任何真实的服务器地址或个人信息。示例一律使用 `memory.example.com`、`example.invalid` 这类保留域名。

## 需要格外说明的改动

以下改动会影响数据安全或已有部署，请在 PR 描述里写清影响和迁移方式：

- 权限与项目隔离（`acl`、成员角色、会话、邀请、设备授权）
- 记忆状态流转（提案、采纳、拒绝、过期、撤回）
- 数据库表结构或已有数据的含义
- HTTP API、MCP 工具参数、命令行参数
- 发给向量库或写入日志的数据

## 报告问题

- 功能问题和建议：直接提 Issue，附上版本号（`/health` 返回的 `version`）、复现步骤和期望结果。
- 安全问题：**不要公开提 Issue**，请按 [SECURITY.md](SECURITY.md) 私下报告。

提交代码即表示你同意以 [Apache License 2.0](LICENSE) 授权你的贡献。
